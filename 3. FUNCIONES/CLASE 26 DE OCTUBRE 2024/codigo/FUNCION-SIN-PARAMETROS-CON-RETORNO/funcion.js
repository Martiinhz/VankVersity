/*
Algunas funciones no tienen parámetros pero tienen retorno,
son usadas generalmente para formatear mensajes, cambiar
estados de una varibale entre otros. Retornar valores de constantes
*/
const PI = 3.1416;

function valorPi() {
    return PI;
}

let resultadoPi = valorPi();
console.log(resultadoPi);

